let usuario = "Ana";
let autenticado = false;

if(usuario == true && autenticado == true){
    console.log(`Olá ${usuario} você está autenticado`);
}
else{
    console.log(`Usuário não autenticado`);
}